<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/rbSelected" /> <!-- Color de fondo del botón -->
    <corners android:radius="0dp"/> <!-- Esto asegura que no haya esquinas redondeadas -->
    <stroke android:width="2dp" android:color="@color/rbUnselected"/> <!-- Borde del botón (opcional) -->
</shape>